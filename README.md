# نظام تسجيل نقاط التايكوندو

نظام عربي ثابت للتحكيم وتسجيل النقاط، مع شاشة نتائج مباشرة، ثلاثة حكام فقط، وسجل نزالات قابل للتعديل.

## التشغيل المحلي

1. ثبّت الحزم: `npm install`
2. أنشئ `.env.local` من `.env.example` وأضف إعدادات البدء الآمنة.
3. شغّل: `npm run dev`

## النشر

## GitHub Pages

يُنشر تلقائياً إلى GitHub Pages، ويتصل بقاعدة Cloud Firestore للمشروع `gggg111-c6858` من خلال ملف `src/lib/firebase.ts`. لأن GitHub Pages موقع ثابت، لا تضع فيه مفاتيح خادم أو أسرارًا حقيقية. يجب ضبط قواعد Firestore قبل الاستخدام العام.
